import React from "react";
import classes from "./DivisionBar.module.css";

const divisionBar = props => {
  return <h5 className={classes.DivisionBar}>
    <span>또는</span>
  </h5>;
}

export default divisionBar;
